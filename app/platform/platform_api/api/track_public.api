syntax = "v1"

type (
	TrackReq {
		Logs []map[string]interface{} `json:"logs"`
	}
	TrackRes {}
)

@server (
	group: track_public
)
service platform {
	@doc "记录客户端日志"
	@handler Track
	post /api/platform/track_public/v1/track (TrackReq) returns (TrackRes)
}
