
services:
  track_api:
    image: wsrh8888/track_api:1.0.2
    container_name: track_api
    network_mode: "host"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./track.yaml:/etc/track.yaml:ro
    deploy:
      restart_policy:
        condition: any
    pull_policy: always 
