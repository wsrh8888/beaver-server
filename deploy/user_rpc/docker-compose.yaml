
services:
  user_rpc:
    image: wsrh8888/user_rpc:1.0.0
    container_name: user_rpc
    network_mode: "host"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./userrpc.yaml:/etc/userrpc.yaml:ro
    deploy:
      restart_policy:
        condition: any
    pull_policy: always
