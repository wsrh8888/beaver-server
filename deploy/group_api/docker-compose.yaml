
services:
  group_api:
    image: wsrh8888/group_api:1.0.0
    container_name: group_api
    network_mode: "host"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./group.yaml:/etc/group.yaml:ro
    restart: "no"
    pull_policy: always 
