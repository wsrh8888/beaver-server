
services:
  datasync_api:
    image: wsrh8888/datasync_api:1.0.0
    container_name: datasync_api
    network_mode: "host"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./datasync.yaml:/etc/datasync.yaml:ro
    deploy:
      restart_policy:
        condition: any
    pull_policy: always

