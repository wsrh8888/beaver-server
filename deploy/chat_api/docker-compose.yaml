
services:
  chat_api:
    image: wsrh8888/chat_api:1.0.2
    container_name: chat_api
    network_mode: "host"
    volumes:
      - /etc/localtime:/etc/localtime:ro
      - ./chat.yaml:/etc/chat.yaml:ro
    deploy:
      restart_policy:
        condition: any
    pull_policy: always 
