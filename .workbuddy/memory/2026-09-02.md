# 2026-09-02

## logx → beaverlog 迁移收尾（按服务逐个手工，不批量、Text 中文、Data 放参数）

继续前序会话的逐服务迁移，本轮完成剩余全部业务服务：

- **ws_api**: `response.go`（3 处，无 ctx，函数内 `beaverlog.New("ws_response")`）。✅
- **user_api**: `updateinfologic.go`（7 处 + 删 1 处重复 Infof）。✅
- **call_api**: `livekitwebhooklogic.go`（3 处）。✅
- **notification_api**: `deletenotificationlogic.go` + `registerpushtokenlogic.go`（4 处 + 删 1 处重复 Infof）。✅
- **moment_api**: `createmomentcommentlogic.go` + `likemomentlogic.go`（3 处）。✅
- **file_api**: `file_utils.go`（2）、`fileuploadlocalhandler.go`（2）、`fileuploadqiniuhandler.go`（3）—— handler 用 `beaverlog.New("file_upload_local/qiniu", r.Context())`。✅
- **emoji_api**: `addemojilogic.go`（4）、`updatefavoriteemojilogic.go`（9）、`updatefavoriteemojipackagelogic.go`（5）。✅
- **chat_rpc**: `sendmsglogic.go`（12，含 panic 恢复 `Data{"panic": r}` 直传）、`open_webhook_log_writer.go`（1）、`sync_utils.go`（12，包级 `syncLogger` 变量）。✅
- **gateway_admin / gateway_api**: `core/proxy.go` 全部业务日志转 beaverlog（admin 16 处、api 6 处）；`gateway.go` 仅 admin 中间件的 1 处 Infof 转 beaverlog。

## 关键结论：迁移已全部完成
- `go build ./...` 全仓通过（EXIT=0）。
- 全仓业务代码 logx 调用已清零。剩余 8 处 logx 均为基础设施，属 beaverlog 底层依赖，**须保留**：
  - 两个 `gateway*/gateway.go` 的 `logx.SetUp(config.Log)`（日志写入器初始化，beaverlog 内部依赖 logx）。
  - 两个 `gateway*/types/config.go` 的 `logx.LogConf`（Config 结构体字段类型，供 SetUp 使用）。

## 复用要点（下次同类任务可直接套用）
- 无 ctx 的包级/工具函数：用 `var xLogger = beaverlog.New("module_name")` 包级变量，或函数内 `beaverlog.New("module", ctx)`。
- panic recover 值 `r` 直接进 `Data{"panic": r}`，无需 fmt（Data 是 interface{}）。
- 同文案多处：用 `replace_all=true`；不同处用上下文片段保证唯一。
- 删除重复 Infof 后务必检查是否留下 unused 变量（曾踩过 statusText）。

## 日志可观测性（对标大厂：OTel 采集 → 存储 → 看板）

用户明确：本项目是"对标大厂、为了学习"，不是"能用就行"——架构应走大厂范式（OTel 统一协议 + 队列缓冲 + 索引/列存存储 + 看板/告警），规模可缩。

落地（本轮只接一个服务看效果）：
- 采集栈：`deploy/logging/docker-compose.yaml`（OpenSearch 2.19 + OpenSearch Dashboards + OTel Collector contrib 0.115）；配置 `deploy/logging/otel-collector-config.yaml`（otlp→batch→opensearch，index=beaver-logs，mapping.mode=otlp，安全已关闭仅本机学习用）。
- `utils/beaverlog/logger.go`：`send()` 末尾新增**异步 OTLP 推送**，由 env `BEAVERLOG_OTLP_ADDR` 开关（默认关闭）。仅设了该变量的服务才上报，其他服务不受影响。本地 logx 输出保留作兜底。用官方 OTel logs SDK（非手写 HTTP）。
- 试点服务：`auth_api`（在 `deploy/auth_api/docker-compose.yaml` 加了 `environment: BEAVERLOG_OTLP_ADDR=127.0.0.1:4317`）。注意：需先用含新 beaverlog 的代码**重建 auth_api 镜像**，env 才有意义。
- 新增依赖（独立模块，非主 otel 模块）：`otel/log`、`otel/sdk/log`、`otel/exporters/otlp/otlplog/otlploggrpc` 均 v0.22.0。
- ⚠️ 副作用：`go get` 把 go.mod 的 `go` 指令从 `1.24.13` 升到 **`1.25.0`**（otlploggrpc v0.22.0 要求），并把 otel 核心从 v1.39.0 升到 v1.46.0。沙箱已自动下载 go1.25.0 工具链，编译通过；用户本机若仍是 go1.24 需升级或回退 SDK 版本。

## 下一步（用户未拍板，待续）
- 验证端到端：起 logging 栈 + 重建 auth_api 打日志 → Dashboards :5601 检索 `source=auth_api`。
- 之后可插消息队列（复用现有 RocketMQ 或换 Kafka）做生产-存储解耦/削峰/重放；再上 Kibana 看板与错误率告警；最后补 trace_id 关联。

## 09-02 补充：日志栈并入根基础设施编排
- 用户指出日志后端应放进 `docker/docker-compose.yml`（与 mysql/redis/rocketmq 同属基础设施），而非独立 `deploy/logging/`。
- 已将 opensearch / opensearch-dashboards / otel-collector 三个服务并入 `docker/docker-compose.yml`，沿用 `restart: always` 与 `./xxx` 相对路径约定；OTel 配置移至 `docker/logging/otel-collector-config.yaml`（`volumes: ./logging/otel-collector-config.yaml:/etc/otel/config.yaml:ro`）。
- 已删除独立的 `deploy/logging/`（避免与根编排定义同名容器 beaver-opensearch 等冲突）。`docker compose -f docker/docker-compose.yml config` 校验通过。
- 启动日志后端命令变为：`docker compose -f docker/docker-compose.yml up -d`（一次性拉起全部基础设施 + 日志栈）。

## 09-02 补充：track_public 客户端日志接口重构
- 用户要求把客户端日志接口从独立 `client_log.api` 方案改为**放进 `track_public.api`**，并明确"删除原 /log 端点、改用 /track"。
- 删除原 `LogEvents`（`/api/platform/track_public/v1/log`，埋点入库），重命名为 `Track`（`/api/platform/track_public/v1/track`）。文件 `logeventshandler.go`/`logeventslogic.go` 经 `git mv` 改为 `trackhandler.go`/`tracklogic.go`，符号 `LogEvents*`→`Track*`、`LogEventItem`→`TrackItem`（types.go 同步）。
- 新 `Track` 逻辑**双写**：① 保留埋点入库 `TrackLogger`（原 behavior，呼应"埋点走原属性"）；② 每条日志额外经 `beaverlog.New("frontend_log", ctx).Info(...)` 转发到 OpenSearch（module=frontend_log），供 Dashboards 检索。
- `deploy/platform_api/docker-compose.yaml` 补 `BEAVERLOG_OTLP_ADDR=127.0.0.1:4317`（与 auth_api 一致，限定本服务开启远程日志）。
- `go build ./app/platform/platform_api/...` 通过（EXIT=0），grep 已无 `LogEvents`/旧 `/log` 残留。
- ⚠️ 前端需把上报地址从 `/api/platform/track_public/v1/log` 改为 `/api/platform/track_public/v1/track`。

## 09-02 补充：OTLP 地址从 docker env 改为服务 yaml 配置
- 用户指出 `BEAVERLOG_OTLP_ADDR` 写在 docker-compose 的 environment 里"很奇怪"，应与其 DB/Redis 等基础设施端点一致放在服务 yaml。故迁移。
- `utils/beaverlog/logger.go`：`initRemote()` 不再读 `os.Getenv("BEAVERLOG_OTLP_ADDR")`，改为读包级变量 `otlpAddr`；新增 `beaverlog.SetOtlpAddr(addr string)`（启动时、InitFromConf 之后调用）；移除 `os` 依赖与 env 读取。`otlpAddr` 为空即关闭远程日志（默认）。
- 各服务：config 加 `OtlpAddr string` 字段（与 `rest.RestConf` 同级）；入口 `xxx.go` 在 `InitFromConf` 后加 `beaverlog.SetOtlpAddr(c.OtlpAddr)`；对应 yaml（`platform.yaml`/`auth.yaml`）加 `OtlpAddr: 127.0.0.1:4317`；`deploy/*/docker-compose.yaml` 删除 environment 块；`docker/docker-compose.yml` 注释同步更新。
- 已落地平台/认证两个试点服务。`go build ./app/platform/platform_api/... ./app/auth/auth_api/... ./utils/beaverlog/...` 通过（EXIT=0）。其他未调用 SetOtlpAddr 的服务 otlpAddr 为空，自动保持本地日志，不受影响。
- 注意：仍须先用含新 beaverlog 的代码重建镜像，yaml 里的 OtlpAddr 才生效；采集栈（otel-collector:4317）需先 `docker compose -f docker/docker-compose.yml up -d` 起好。

## 09-02 补充：客户端日志走"网关 + 透传"（SLS 匿名范式，不直接连 OpenSearch）
- 用户拍板：① `/track` 去掉 TrackLogger DB 双写，纯透传至 OpenSearch；② 现在就加网关护栏（写令牌 + 限流 + 可信字段）。
- **去 DB 双写**：`tracklogic.go` 删除 `DB.Transaction` + `TrackLogger` 写入，仅经 `beaverlog.New("client_log", ctx).Info(...)` 转发 OpenSearch；同步移除 gorm/datatypes/platform_models 依赖。module 由 `frontend_log` 改为 `client_log`，OpenSearch 检索用 `source=platform AND module=client_log`。
- **护栏一·写令牌**：请求头 `X-Log-Token` 经 go-zero `header:"X-Log-Token,optional"` 绑到 `TrackReq.Token`（与现有 `header:"Beaver-User-Id"` 同机制）。config 加 `ClientLogTokens []string` + yaml `ClientLogTokens: ["beaver-client-log-2026"]`；非空则必须命中，空列表则开发态放行（fail-soft）。
- **护栏二·限流**：按 token 固定窗口 1 分钟，sync.Map 内存态（`clientLogLimiter`）；config `ClientLogRateLimit int` + yaml `ClientLogRateLimit: 600`。单实例内存态，多实例需换 Redis。
- **护栏三·字段上限**：单次 ≤200 条、单条 data ≤16KB、level/bucketId ≤256；超限整体拒绝。
- **可信字段**：handler 取客户端 IP（`X-Forwarded-For` 优先，否则 `RemoteAddr`）经 `context.WithValue(logic.ClientIPCtxKey, ...)` 注入，logic 作为不可伪造的 `ip` 写入；客户端上报内容一律当不可信 `data` 透传，不信任其 source/module/时间（防伪造 `source: auth_api` 污染）。
- `go build ./app/platform/platform_api/...` 通过（EXIT=0），grep 无 TrackLogger/DB 残留。
- **同轮撤销**：用户决定令牌校验 + 限流交由前置网关处理，故移除应用内这两层——删掉 `ClientLogTokens`/`ClientLogRateLimit` 配置（config.go + platform.yaml）、`TrackReq.Token`（`X-Log-Token` 头字段，types.go + track_public.api）、logic 中的令牌校验与按 token 限流及 `rlEntry`/`allowClientLog`/`sync`/`time`/`errors` 依赖。保留字段上限（单次≤200条、data≤16KB、level/bucketId≤256）作为防索引映射被撑爆的兜底。前端 SDK：POST `/api/platform/track_public/v1/track`，body `{logs:[{level,bucketId,data,timestamp}]}`（无需 X-Log-Token 头，鉴权限流归前置网关）。
- 用户希望客户端调用写法对标阿里云 SLS 匿名上报（`LogWriter` 类 POST 数组）。已新增客户端 SDK 参考实现：`clients/electron/log_writer.ts`（axios，镜像用户原 LogWriter 写法）、`clients/flutter/log_writer.dart`（http 包）。契约与服务端 `/track` 对齐：`post {endPoint}`，`data:{logs:[...]}`，无鉴权头。注意这是 reference，需拷进各自前端工程（仓库内无现成前端工程）。

## 09-02 补充：客户端日志接口改为 SLS 自由字段风格（去 bucketId、去包装）
- 用户指出 OpenSearch Dashboards **没有"桶"概念**：阿里云 SLS 的 `doubleteacher_pc` 是 logstore 名，对应 OpenSearch 的 **index**（本项目固定 `beaver-logs`，见 `docker/logging/otel-collector-config.yaml`），由服务端决定，客户端不该传。原 `bucketId` 字段是冗余发明，删除。
- 用户要求"很多参数不需要"：原前端把整条结构化日志 `JSON.stringify` 塞进 `data` 字符串，改为**直接透传扁平对象**（SLS `__logs__` 风格）。
- **服务端**：`track_public.api` / `types.go` 删除 `TrackItem`，`TrackReq.Logs` 改为 `[]map[string]interface{}` / `[]map[string]any`（go-zero v1.7.4 支持）。`tracklogic.go` 改为：遍历 map → 服务端注入可信 `ip`（`ClientIPCtxKey`）→ 按 `level` 字段映射 beaverlog.Info/Warn/Error → `model.LogMsg{Text, Data: item}` 直接透传；保留量级(≤200)/单条体积(≤16KB)上限。
- **前端 beaver-desktop**（真实工程，非参考）：`main/utils/log/index.ts` 去掉 `getLogId`/`bucketId`，`write` 直接 `logEventsApi({logs: items})`（items 即 `formatLog` 的扁平对象）；`common/type/ajax/track.ts` 用 `ILogItem=Record<string,unknown>` 替换 `ILogData`；`main/api/track.ts` 与 `render/api/track.ts` 的 URL 由 `/track_public/v1/log` 改为 `/track_public/v1/track`。`getLogId` 定义保留（仅此处不再使用）。
- **删除参考 SDK**：`clients/electron/log_writer.ts`、`clients/flutter/log_writer.dart` 及空目录 `clients/` 已删（用户要求删，前端直改真实工程）。
- 编译：`go build ./app/platform/platform_api/...` EXIT=0；grep 无 `TrackItem|BucketID|bucketId|track_public/v1/log` 残留。

## 09-02 补充：本地启动日志栈实测（Desktop/docker）踩坑与修复
- 用户要求在 `C:/Users/test/Desktop/docker/docker-compose.yaml` 加 opensearch 配置并启动看效果（仅起日志三件套，不动 mysql/postgres/jenkins 等）。
- **实测可用的镜像源**：该环境只能访问 daocloud(`docker.m.daocloud.io`) 与华为云 SWR(`swr.cn-north-4.myhuaweicloud.com/ddn-k8s/docker.io/...`)；docker.io 直连 `connection refused`、中科大 `docker.mirrors.ustc.edu.cn` DNS 解析失败。
  - `opensearch`：`docker.m.daocloud.io/opensearchproject/opensearch:2.19.0` ✅ 可拉。
  - `opensearch-dashboards`：`docker.m.daocloud.io/opensearchproject/opensearch-dashboards:2.19.0` ✅ 可拉。**注意命名空间 `opensearchproject`、镜像名必须带 `-dashboards`；少写 `-dashboards` 会用成 opensearch 镜像本身，dashboards 容器必然异常。**
  - `otel/opentelemetry-collector-contrib:0.115.0`：daocloud 403、华为云 SWR not found/需登录、docker.io 不通 —— **当前网络拉不到**，故日志采集链路暂缺 collector 一段（4317 收不到）。
- **关键修复1（数据目录权限）**：opensearch 用 `./opensearch/data` bind mount 在 Windows+Docker Desktop 报 `AccessDeniedException: /usr/share/opensearch/data/nodes`（容器 uid 1000 无法在宿主机目录建子目录）。改 **named volume** `opensearch-data:/usr/share/opensearch/data`（Docker 托管权限）解决。
- **关键修复2（登录墙）**：2.19 禁用 Dashboards 安全的变量是 **`DISABLE_SECURITY_DASHBOARDS_PLUGIN=true`**（不是 `DISABLE_SECURITY_DASHBOARDS`）。写错会让 Dashboards 仍显示登录页（API 401）。opensearch 侧 `plugins.security.disabled=true` 已生效（9200 可连）。
- **验证结果**：opensearch `healthy`（9200）、dashboards `running` 且访问 referer 到 `/app/home`（无登录墙，可直接进 Discover）。用户可在 `http://localhost:5601` 看效果。
- ⚠️ **仓库版同步提醒**：`beaver-server/docker/docker-compose.yml` 里我此前写入的日志栈同样有这两处隐患（`./opensearch/data` bind mount + `DISABLE_SECURITY_DASHBOARDS=true`），用户若要正式跑仓库版需一并改为 named volume + 正确变量名；且 otel-collector 镜像在其网络环境也需确认可拉。
- **09-02 晚：collector 已拉取并启动**。用户在自有 PC 网络下成功 `docker pull otel/opentelemetry-collector-contrib` 并 `docker compose up -d otel-collector`（实际跑的是 0.159.0 版本，配置兼容）。实测验证「collector→OpenSearch」半链路：从 opensearch 容器 POST 测试日志到 `otel-collector:4318/v1/logs` 返回 200，`beaver-logs` 索引自动创建并搜到文档（`module=self_test` 落在顶层字段）。**当前链路仅剩 `platform_api(beaverlog OTLP)→127.0.0.1:4317` 这一段未验证**（需用户本地起 platform_api 并 POST `/api/platform/track_public/v1/track`，再在 Dashboards Discover 搜 `module=client_log` 确认）。验证 POST 示例：`curl -X POST localhost:21600/api/platform/track_public/v1/track -H 'Content-Type: application/json' -d '{"logs":[{"level":"info","message":"hello","module":"test"}]}'`。

## 09-02 补充：beaverlog 平铺 data + 修 message 双重 JSON + 仓库版 compose 同步
- 用户确认执行上轮末尾提出的三项收尾（"你改改"）。
- **beaverlog emitRemote 平铺 data**：`utils/beaverlog/logger.go` 原把整个 `msg.Data` 序列化为单个 `data` 字符串 attribute（导致 Dashboards 无法按 platform/deviceId/level 过滤）。改为遍历 `msg.Data`（类型断言为 `map[string]any`，非 map 时退化回整体 data 字符串以兼容服务端结构体日志），逐键平铺为独立 OTLP attribute；新增 `toLogAttr()` 做类型安全转换（string→String / bool→Bool / int 系→Int64 / float 系→Float64 / map·slice→JSON 字符串兜底）。`source`/`module` 由 beaverlog 注入，若 Data 自带同名键则让位给 Data（客户端日志场景 Data 的 source/module 才是业务来源），避免重复 key。`rec.SetBody` 优先取 `Data.message` 作日志正文（透传场景下 msg.Text 恒为"客户端日志上报"），否则回退 msg.Text。需新增 `import "fmt"`（toLogAttr 兜底）。`go build ./utils/beaverlog/` EXIT=0。
- **前端 message 双重 JSON 修复**：`beaver-desktop/src/main/utils/log/index.ts` 的 `formatLog` 原 `message: \`${JSON.stringify(msg)}\`` 把整个 ILogger（含 data）stringify 进 message 字符串，造成双重 JSON 且业务 data 内字段（userId/wsUrl/error 等）不可过滤。改为 `message: msg?.text ?? ''`，并把 `msg.data`（对象时）展平到 item 顶层（`...extra`），对齐 SLS 自由字段风格——既消除双重 JSON，又让业务附加字段可被 OpenSearch 检索/过滤。本地 console/log4js 输出的 JSON.stringify 保持不变（仅改上报体）。
- **仓库版 compose 同步修坑**：`docker/docker-compose.yml` ① opensearch `./opensearch/data` bind mount 改 named volume `opensearch-data:/usr/share/opensearch/data`（文件末尾新增 `volumes:\n  opensearch-data:` 段），避免 Windows/macOS 权限问题；② `DISABLE_SECURITY_DASHBOARDS=true` → `DISABLE_SECURITY_DASHBOARDS_PLUGIN=true`（去登录墙）。`yaml.safe_load` 校验通过。
- ⚠️ 验证待用户本地完成：需重启 beaver-desktop（让前端改动生效）与重跑 `go run platform.go`（beaverlog 改了需重编译运行），触发一次客户端日志后，在 Dashboards Discover（`beaver-logs*`）确认 `platform`/`deviceId`/`level`/`message` 均为顶层字段、message 为纯文本（不再双重 JSON）。
