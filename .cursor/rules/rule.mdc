---
alwaysApply: true
---
# 项目概述

这是一个开源的IM（即时通讯）项目，对标大厂和开源IM产品。本项目使用分为3层

### 技术架构
go+go-zero


#### .api文件规范
1、每个接口的入参和返回值都写在一个独立的type()块中，不要把所有接口的类型放在一起，有6个接口有且只能有6个type， 如果3个接口只能有3个type， 接口是有几个@handler 就几个接口
2、出参需要的额外的参数也包裹在这个type里，但是要加一个特殊的名字，防止重名， 也
3、注释要清晰说明每个字段的含义和数据类型
4、不要和别的接口共享参数，不然改A的时候B也改了，每一个接口应该是独立的。


### go-zero 规范
1、 handler目录下的是一些接口入参的校验（参数校验）
   - 基础参数校验：非空校验、格式校验、长度校验、范围校验等
   - 例如：参数不能为空、日期格式校验、分页参数校验等
   - 参数校验失败直接返回错误，不进入logic层
2、 logic目录下的是一些接口逻辑的实现（业务逻辑）
   - 业务逻辑：数据库操作、业务规则校验等
   - 例如：创建/更新/删除数据等
   - 不要在这里做基础参数校验（应该在handler层完成）
3、 我项目有一个中间件目录，里面对返回值进行了包裹{code: 0, msg: '成功', result: {数据}}, 各个业务只返回{数据}，


### handler 规范
- handler里是写一些校验相关的， 比如req的传入参数是否合法

### logic 规范
- logic里是写一些业务逻辑相关的， 比如数据库操作、第三方接口调用等

### 命名规范
- .api 文件：使用 驼峰命名（CamelCase），与现代 API 设计和前端接口一致。
- .rpc 文件：使用 下划线命名（snake_case），符合传统 RPC 和 Protobuf 的命名习惯。
- 数据库表名使用 下划线命名（snake_case），符合传统数据库表的命名习惯。